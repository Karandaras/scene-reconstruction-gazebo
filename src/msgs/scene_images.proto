package gazebo.msgs;

/// \ingroup gazebo_msgs
/// \interface Scene Images
/// \brief Images from MongoDB for SceneObjectData messages
/// \verbatim

import "image.proto";

message SceneImages
{
  required string objectid          = 1;
  required string name              = 2;
  required Image image              = 3;
}

