package gazebo.msgs;

/// \ingroup gazebo_msgs
/// \interface Scene Object Data
/// \brief Objectdata for the ObjectInstantiatorPlugin from the Framework
/// \verbatim

import "scene_images.proto";

message SceneObjectData
{
  required string objectids         = 1;
  repeated SceneImages images       = 2;
}

