package gazebo.msgs;

/// \ingroup gazebo_msgs
/// \interface BufferPosition 
/// \brief Message that is used to hold the data of the robot position buffer
/// \verbatim

import "pose.proto";

message BufferPosition
{
  required double timestamp      = 1;
  required Pose position         = 2;
}

/// \endverbatim
