package gazebo.msgs;

/// \ingroup gazebo_msgs
/// \interface Scene Robot
/// \brief Control the Robot via the RobotControllerPlugin
/// \verbatim

import "pose.proto";

message SceneRobot
{
	required Pose   pose              = 1;
  optional double controltime       = 2;
}

